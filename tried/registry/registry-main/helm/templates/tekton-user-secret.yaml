apiVersion: v1
kind: Secret
metadata:
  labels:
    app.kubernetes.io/instance: default
    app.kubernetes.io/part-of: ertia
  name: tekton-robot
  namespace: registry
type: Opaque
data:
  {{- $token := .Values.tektonRobotToken | default (randAlphaNum 128) | b64enc | quote }}
  token: {{ $token }}
