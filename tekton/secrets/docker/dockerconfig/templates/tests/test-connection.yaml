apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dockerconfig.fullname" . }}-test-connection"
  labels:
    {{- include "dockerconfig.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dockerconfig.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
