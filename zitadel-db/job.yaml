apiVersion: batch/v1
kind: Job
metadata:
  name: zitadel-db-user-job
spec:
  template:
    spec:
      containers:
        - name: pg-client
          image: postgres
          env:
            - name: PGPASSWORD
              value: "stolon"
          command: ["psql", "-h", "stolon-proxy", "-U", "stolon","-d","postgres","-c", "CREATE USER zitadel"]
      restartPolicy: Never
  backoffLimit: 4
---
apiVersion: batch/v1
kind: Job
metadata:
  name: zitadel-db-job
spec:
  template:
    spec:
      containers:
        - name: pg-client
          image: postgres
          env:
            - name: PGPASSWORD
              value: "stolon"
          command: ["psql", "-h", "stolon-proxy", "-U", "stolon","-d","postgres","-c", "CREATE DATABASE zitadel"]
      restartPolicy: Never
  backoffLimit: 4
---
apiVersion: batch/v1
kind: Job
metadata:
  name: zitadel-db-grant-job
spec:
  template:
    spec:
      containers:
        - name: pg-client
          image: postgres
          env:
            - name: PGPASSWORD
              value: "stolon"
          command: ["psql", "-h", "stolon-proxy", "-U", "stolon","-d","postgres","-c", "GRANT ALL PRIVILEGES ON DATABASE zitadel TO zitadel"]
      restartPolicy: Never
  backoffLimit: 4
